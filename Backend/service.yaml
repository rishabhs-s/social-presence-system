apiVersion: v1
kind: Service
metadata:
  name: backendapp
spec:
  selector:
    app: backendapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort  # Change to ClusterIP or LoadBalancer based on your requirements
